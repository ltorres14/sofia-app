class AppConstants {
  static const String appName = 'SOFIA Check';
  static const Duration requestTimeout = Duration(seconds: 20);
  static const Duration refreshInterval = Duration(seconds: 10);
  static const int pinLength = 4;
}
